<!DOCTYPE html>

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Recipes</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400&display=swap"
            rel="stylesheet"
        />
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="stylesheet" href="/styles/style.css" />
        <link rel="stylesheet" href="/styles/tailwind.css" />
    </head>

    <body>
        <header class="flex h-16 items-center justify-between gap-2">
            <button
                class="h-12 flex-none dark-red text-white flex justify-center items-center m-2 p-2 rounded-xl w-12"
            >
                R
            </button>

            <form class="flex flex-1 justify-between items-center">
                <input
                    type="text"
                    id="search"
                    class="h-12 flex-1 p-2 rounded-xl text-lg grow"
                    placeholder=""
                />
                <button
                    class="h-12 flex-initial m-1 p-2 rounded-xl bg-white w-12 flex items-center justify-center"
                >
                    <img src="/svg/search.svg" alt="Search" />
                </button>
            </form>

            <button
                class="h-12 dark-red text-white text-center m-2 p-2 rounded-xl w-48 font-thin text-xs md:text-lg sm:text-base lg:text-xl truncate"
            >
                Sign up or Login
            </button>
        </header>
        <!-- Render script for logged in users only -->
        {{#if logged_in}}
        <script src="/js/logout.js"></script>
        {{/if}}
    </body>
</html>
